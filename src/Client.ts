/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "./environments";
import * as core from "./core";
import { Cyclone } from "./api/resources/cyclone/client/Client";
import { Weather } from "./api/resources/weather/client/Client";

export declare namespace EarthClient {
    export interface Options {
        environment?: core.Supplier<environments.EarthEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        apiKey: core.Supplier<string>;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string>;
    }
}

export class EarthClient {
    protected _cyclone: Cyclone | undefined;
    protected _weather: Weather | undefined;

    constructor(protected readonly _options: EarthClient.Options) {}

    public get cyclone(): Cyclone {
        return (this._cyclone ??= new Cyclone(this._options));
    }

    public get weather(): Weather {
        return (this._weather ??= new Weather(this._options));
    }
}
